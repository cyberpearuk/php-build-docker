sudo: required

services:
  - docker

script:
    - bash build.sh $TRAVIS_REPO_SLUG latest

deploy:
    provider: script
    script:
        - bash deploy.sh $TRAVIS_REPO_SLUG latest
    on:
        branch: master
